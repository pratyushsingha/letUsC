#include <stdio.h>
int main()
{
    int x, y, r;
     printf("Enter the coordinates(x,y) of center of a circle: ");
     scanf("%d %d", &x, &y);
        printf("Enter the radius of circle: ");
        scanf("%d", &r);
        
return 0;
}